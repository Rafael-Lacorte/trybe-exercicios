import React, { useContext } from 'react';

function Email({ emailInfo, setEmailInfo}) {
  const { name } = this.props;
  return (
        <main className="cart-wall d-flex flex-column align-items-center">
            <p>{name}</p>
            {/* <p>{status}</p> */}
            <p>Vrau</p>
        </main>
  );
}

export default Email;
